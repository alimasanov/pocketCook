<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pocketcook.alim.pocketcook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityFridgeActivity"
            android:label="@string/holodilnic_label"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ActivitySettingsActivity"
            android:label="@string/title_activity_settings_activity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ActivityMenuActivity"
            android:label="@string/menu_resourse_name"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ActivityAboutActivity"
            android:label="@string/about_title"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ActivitySettingsRecipe"
            android:label="@string/settings_title"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ActivitySettingsIngridient"
            android:label="@string/title_activity_settings_ingridient"
            android:theme="@style/AppTheme" />
        <activity android:name=".ActivityIngrAdd" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>